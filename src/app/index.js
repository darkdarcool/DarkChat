/**
 * 
 * @param {import("firebase/auth").User} user 
 */

import terminal from "../services/terminal.js";

export default async function app(user) {
  terminal.clear(); // for login/signup
  while (true) {

  }
}